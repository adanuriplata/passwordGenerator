import { Password } from "./types/password"

// export const GeneratePassword = (  lenght: number, upperCase: boolean, lowerCase: boolean, numbers: boolean, symbols: boolean, ): string => {
//   return 'hola'
// }


export const GeneratePassword = ( paswordOptions:Password ) => {
  return paswordOptions
}